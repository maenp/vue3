import * as $com from './../util/common'
import $nativeBridge from './../util/nativeBridge'
import { } from 'vue'

export const useUtil = () => {
    return {
        $com,
        $nativeBridge,
    }
};